version: "3.9"

services:
  ms_route-tes: 
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ms_routes-test
    env_file:
      - ./variables.env
    ports:
      - "5000:5000"
      - "5672:5673"
    command: npm start
 