export default class VehicleInformation {
    constructor(brand, model, line, plate, color, doors) {
        this.brand = brand;
        this.model = model;
        this.line = line;
        this.plate = plate;
        this.color = color;
        this.doors = doors;
    }
}